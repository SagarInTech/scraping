services:
  - type: web
    name: hotel-scraper
    env: python
    buildCommand: |
      apt-get update && \
      apt-get install -y chromium-browser && \
      pip install -r requirements.txt
    startCommand: python app.py
    plan: free
